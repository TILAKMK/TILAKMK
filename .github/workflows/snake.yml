name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Generate snake
        uses: Platane/snk@v3
        with:
          github_user_name: TILAKMK
          outputs: |
            dist/github-contribution-grid-snake.svg?palette=github&snake_color=#2ea043
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&snake_color=#2ea043

      - name: Push snake
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist
          publish_branch: output
